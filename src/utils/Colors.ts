export enum Colors {
  BLACK = '#222',
  WHITE = '#FFF',
  YELLOW = '#D2FF37',
  BLUE = '#46D',
  GRAY = '#333'
}

export enum ColorExtensions {
  BLACK = '-black-app',
  WHITE = '-white',
  YELLOW = '-yellow-app',
  BLUE = '-blue-app',
  GRAY = '-gray-app'
}

export enum TextColors {
  black = 'text-black-app',
  white = 'text-white',
  yellow = 'text-yellow-app',
  blue = 'text-blue-app',
  gray = 'text-gray-app'
}

export enum BackColors {
  black = 'bg-black-app',
  white = 'bg-white',
  yellow = 'bg-yellow-app',
  blue = 'bg-blue-app',
  gray = 'bg-gray-app'
}
